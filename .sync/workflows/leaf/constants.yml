# A workflow to to set constants used across the leaf workflows.
# NOTE: This file is automatically synchronized from Mu DevOps to keep the versions of the
#       constants in this workflow up to date. Update the original file there instead of
#       the file in this repo.
#
# - Mu DevOps Repo: https://github.com/microsoft/mu_devops
# - File Sync Settings: https://github.com/microsoft/mu_devops/blob/main/.sync/Files.yml
#
# Copyright (c) Microsoft Corporation.
#
# SPDX-License-Identifier: BSD-2-Clause-Patent
#
{% import '../Version.njk' as sync_version -%}
{% raw %}
# NOTE: Because this pipeline YAML file is a Nunjucks template, the pipeline syntax of `{{}}` will conflict with
#       Nunjucks style. Surround pipeline YAML code that uses `{{}}` within `raw` and `endraw` tags
#       to allow it to pass through Nunjucks processing.
on:
  workflow_call:
    outputs:
      container-image:
        description: 'Container image to use for Linux builds'
        value: ${{ jobs.vars.outputs.container-image }}
      python-version:
        description: 'Version of Python to use when not in a container'
        value: ${{ jobs.vars.outputs.python-version }}
      qemu-core-count:
        description: 'Number of CPU cores for QEMU.'
        value: ${{ jobs.vars.outputs.qemu-core-count }}

{% endraw %}
env:
  CONTAINER_IMAGE: "{{ sync_version.linux_build_container }}"
  PYTHON_VERSION: "{{ sync_version.python_version }}"
  QEMU_CORE_COUNT: '2'
  CURRENT_RELEASE: "{{ sync_version.latest_mu_release_branch }}"
{% raw %}

jobs:
  vars:
    name: Get Repository Constants

    runs-on: ubuntu-latest
    outputs:
      container-image: ${{ steps.set-vars.outputs.container-image }}
      python-version: ${{ steps.set-vars.outputs.python-version }}
      qemu-core-count: ${{ steps.set-vars.outputs.qemu-core-count }}
      current-release: ${{ steps.set-vars.outputs.current-release }}

    steps:
    - name: Set Environment Constants
      id: set-vars
      shell: bash
      run: |
        echo "container-image=${CONTAINER_IMAGE}" >> $GITHUB_OUTPUT
        echo "python-version=${PYTHON_VERSION}" >> $GITHUB_OUTPUT
        echo "qemu-core-count=${QEMU_CORE_COUNT}" >> $GITHUB_OUTPUT
        echo "current-release=${CURRENT_RELEASE}" >> $GITHUB_OUTPUT
{% endraw %}